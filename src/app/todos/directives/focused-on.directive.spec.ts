import { FocusedOnDirective } from './focused-on.directive';

describe('FocusedOnDirective', () => {
  it('should create an instance', () => {
    const directive = new FocusedOnDirective(undefined);
    expect(directive).toBeTruthy();
  });
});
